# conditioned-celeba-latent-diffusion

This is a Conditioned Latent Diffusion model trained on the CelebA Dataset. When given a text description, it should output a face that matches the description.
